CREATE DATABASE formulario;
USE formulario;

CREATE TABLE respostas(
    id INT AUTO_INCREMENT PRIMARY KEY,
    corFone VARCHAR(255) NOT NULL,
    abafadorFone VARCHAR(255) NOT NULL,
    adesivarFone VARCHAR(255) NOT NULL,
    tamanhoFone VARCHAR(255) NOT NULL,
    
);